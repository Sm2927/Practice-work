#include<bits/stdc++.h>
using namespace std;
int main(){
    long long int n;
    cin>>n;
    int d;
    d = floor(log10(n)+1); //to find the number of digits in the binary representation of the number write: d = floor(log2(n)+1);
    cout<<d;
   

}
